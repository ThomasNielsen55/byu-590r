# Terraform best practices:
# - Do NOT commit: state (*.tfstate, *.tfstate.*), *.tfvars (may contain secrets), .terraform/ (local provider binaries).
# - DO commit: .terraform.lock.hcl so everyone and CI use the same provider versions (do not add it below).

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Do NOT add .terraform.lock.hcl here; commit it so everyone uses the same provider versions.
